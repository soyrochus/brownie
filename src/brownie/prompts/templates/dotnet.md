# .NET Project Instructions

Focus on:
- Entry points: `Program.cs`, `Startup.cs`.
- Frameworks: ASP.NET Core, minimal APIs, MVC.
- Structure: `Controllers/`, `Services/`, `Models/`.

Evidence to extract:
- Host configuration and DI container setup.
- Routing endpoints and controllers.
- Data models and EF Core contexts.
- Integrations and background services.

Doc depth:
- Cite concrete classes and endpoints.
- Map request pipeline and service dependencies.
