# Node.js Project Instructions

Focus on:
- Entry points: `package.json` scripts, `index.js`, `server.js`, `app.js`.
- Frameworks: Express, NestJS, Koa, Next.js.
- Structure: `src/`, `server/`, `apps/`, `packages/`.

Evidence to extract:
- Server initialization and middleware pipeline.
- Routing and controller definitions.
- Data models/schemas (Mongoose/Prisma/TypeORM).
- External integrations (SDKs, HTTP clients, queues).

Doc depth:
- Cite concrete modules/functions/routes.
- Map request lifecycle and key service boundaries.
- Highlight configuration and environment usage.

## Probe Terms
- express
- fastify
- koa
- nest
- next
- router
- middleware
